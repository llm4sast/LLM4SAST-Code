A String is being converted to upper or lowercase, using the platform's default encoding. This may result in improper conversions when used with international characters. Use String.toUpperCase(Locale l), String.toLowerCase(Locale l) instead.