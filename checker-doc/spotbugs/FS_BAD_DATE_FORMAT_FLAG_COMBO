The format string includes a bad combination of flags which may lead to unexpected behavior, e.g., using a week year ("Y") with month in year ("M") and day in month ("d") without specifying week in year ("w"). Flag ("y") may be preferable here instead.